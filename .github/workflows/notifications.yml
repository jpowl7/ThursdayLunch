name: Check notifications
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping notification endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://ilikelunch.com/api/cron/notifications
